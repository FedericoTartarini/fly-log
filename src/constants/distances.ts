// Reference distances in kilometres, shared by the distance stats card and the
// badge thresholds so the two can never disagree about how far the Moon is.
export const EARTH_CIRCUMFERENCE = 40075;
export const DISTANCE_TO_MOON = 384400;
export const DISTANCE_TO_MARS = 227_940_000;
